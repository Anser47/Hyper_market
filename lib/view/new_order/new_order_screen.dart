import 'package:flutter/material.dart';

class ScreenNewOrder extends StatelessWidget {
  const ScreenNewOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('new order'),
      ),
    );
  }
}
