import 'package:flutter/material.dart';

class TodayOrderScreen extends StatelessWidget {
  const TodayOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('todays order'),
      ),
    );
  }
}
