import 'package:flutter/material.dart';

class TodaysSummeryScreen extends StatelessWidget {
  const TodaysSummeryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('todays summery'),
      ),
    );
  }
}
